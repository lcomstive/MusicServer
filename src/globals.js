const MusicPath = `${__dirname}/../music/`

module.exports =
{
	MusicPath: 	  `${MusicPath}`,
	QueuePath: 	  `${MusicPath}queue.json`,
	SettingsPath: `${MusicPath}settings.json`,

	queue: [],
	settings: undefined
}
